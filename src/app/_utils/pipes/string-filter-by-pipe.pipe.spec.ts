import { StringFilterByPipePipe } from './string-filter-by-pipe.pipe';

describe('StringFilterByPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StringFilterByPipePipe();
    expect(pipe).toBeTruthy();
  });
});
